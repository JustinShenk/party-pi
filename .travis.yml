language: python

os:
  - linux
  - osx

git:
  depth: 1

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update          ; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install graphviz; fiaddons:

apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libstdc++-4.9-dev
python:
  - "3.5"
  - "3.6"
install:
  - python setup.py install 
script:
  - cd partypi && python main.py 
